[project]
name = "inventory-api-lambda"
version = "1.0.0"
description = "Python Lambda backend for kitchen inventory"
requires-python = ">=3.10"
dependencies = [
  "bcrypt==4.1.3",
  "PyJWT==2.9.0",
  "pymongo==4.8.0",
  "python-dotenv==1.0.1",
]
